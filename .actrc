# Configuração do act para testes locais de GitHub Actions

# Usar imagem otimizada para act
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Arquitetura Linux (necessário no Windows)
--container-architecture linux/amd64

# Arquivo de secrets (não commitar!)
--secret-file .github/.secrets

# Verbose (descomente para debug)
# -v
